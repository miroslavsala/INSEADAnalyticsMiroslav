# Install packages and run libraries
if("pacman" %in% rownames(installed.packages()) == FALSE) {install.packages("pacman")} # Check if you have universal installer package, install if not
if("rpart.plot" %in% rownames(installed.packages()) == FALSE) {install.packages("rpart.plot")} # Check if you have universal installer package, install if not
if("xgboost" %in% rownames(installed.packages()) == FALSE) {install.packages("xgboost")} # Check if you have universal installer package, install if not
if("randomForest" %in% rownames(installed.packages()) == FALSE) {install.packages("randomForest")} # Check if you have universal installer package, install if not
if("glmnet" %in% rownames(installed.packages()) == FALSE) {install.packages("glmnet")} # Check if you have universal installer package, install if not
if("glmnetUtils" %in% rownames(installed.packages()) == FALSE) {install.packages("glmnetUtils")} # Check if you have universal installer package, install if not
if("corrplot" %in% rownames(installed.packages()) == FALSE) {install.packages("corrplot")} # Check if you have universal installer package, install if not
if("vcd" %in% rownames(installed.packages()) == FALSE) {install.packages("vcd")} # Check if you have universal installer package, install if not
pacman::p_load("caret","partykit","ROCR","lift","rpart","e1071")
pacman::p_load("caret","ROCR","lift","glmnet","MASS","e1071")
library(rpart.plot)
library(xgboost)
library(randomForest)
library(pacman)
library(glmnet)
library(glmnetUtils)
library(corrplot) #package corrplot
library(vcd)
